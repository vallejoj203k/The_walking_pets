name: the_walking_pets
description: Aplicación de paseo de mascotas - The Walking Pets
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^2.3.0
  provider: ^6.1.1
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  uuid: ^4.3.3
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  intl: ^0.20.2
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
